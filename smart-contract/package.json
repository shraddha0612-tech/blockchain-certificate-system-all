{
  "name": "certchain-smart-contract",
  "version": "1.0.0",
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomiclabs/hardhat-ethers": "^2.1.0",
    "ethers": "^6.0.0",
    "@nomicfoundation/hardhat-toolbox": "^2.0.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia"
  }
}